#include <stdio.h>

// Function to add two integers
int add_i32(int a, int b) {
    return a + b;
}

// Function to add two doubles (f64 in Rust)
double add_f64(double a, double b) {
    return a + b;
}
