# Aside on VMs

## Recommendations
- Briefly explain the role of **Virtual Machines (VMs)** in enabling polyglot execution, where multiple languages run within a unified runtime environment.
- Connect VMs to **language interoperability**, emphasizing how VMs abstract away differences in type systems across languages.
- Introduce **GraalVM** as an example of a polyglot VM that compiles multiple languages to a common **intermediate representation (IR)**, enabling seamless interaction between different language ecosystems.
- Discuss how GraalVM uses **metacompilation** (compiling compilers) and **supercompilation** (advanced program optimization techniques) to optimize execution across languages, and relate this to **program transformation** for bridging complex type systems.
- Explain how **tracing JIT compilation** in GraalVM dynamically optimizes execution paths based on runtime data, which could be an analogy for reconciling type systems across languages.
- Mention **intermediate representations (IRs)** used by VMs like GraalVM and how they serve as a common language between languages with different type systems, making them more interoperable.
- Connect the use of IRs in VMs to the challenges of **type system interoperability**, suggesting that an intermediate type model might be used to enable communication between languages with complex, heterogeneous type systems.
- Briefly reference **Microsoft’s CLR** and **.NET** as an example of another VM that enables interoperability between languages by using a unified runtime.
- Conclude by tying VMs to your thesis: how GraalVM and other VMs offer a potential model for abstracting away type system differences and creating a **unified execution model** for polyglot systems.


## Draft

## Refined Draft 

## Final Draft