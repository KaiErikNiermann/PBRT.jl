\begin{tikzpicture}[subgraph text bottom=text centered,
    subgraph nodes=black]
    \graph [tree layout, nodes=draw, component align=center] {
    %   a -> { b -> [dashed] {c, d}, e -> {f, g -> h} };
  
    %   left [draw]  // { b, c, d };
    %   right [draw] // { e, f, g, h};
  
    %   left <-> right;
    };
  \end{tikzpicture}